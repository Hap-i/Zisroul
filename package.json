{
  "name": "root",
  "private": true,
  "scripts": {
    "start:dev": "lerna run start --ignore server-template",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean --yes",
    "build": "lerna run --ignore server-template --sort --include-dependencies build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.4.3"
  }
}
