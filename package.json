{
  "name": "root",
  "private": true,
  "scripts": {
    "start:dev": "lerna run start",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean --yes",
    "build": "lerna run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.4.3"
  }
}
